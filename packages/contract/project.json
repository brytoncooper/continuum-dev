{
  "name": "contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/contract/src",
  "projectType": "library",
  "tags": ["scope:contract"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "packages/contract",
        "commands": [
          "tsc --build --clean tsconfig.lib.json",
          "tsc --build tsconfig.lib.json"
        ]
      },
      "outputs": ["{workspaceRoot}/dist/packages/contract"]
    },
    "nx-release-publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["^nx-release-publish"],
      "options": {
        "packageRoot": "dist/packages/contract"
      }
    }
  }
}
