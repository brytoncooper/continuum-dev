# `packages/ai-engine/src`

Internal source for `@continuum-dev/ai-engine`. **Consumers** import from `@continuum-dev/ai-engine`, not from paths under `src/`.

**Start here:** [`lib/ARCHITECTURE.md`](lib/ARCHITECTURE.md) — map of every `lib/*` folder, public vs internal modules, and how execution flows through the package.

Then open [`index.ts`](index.ts) for the published API, and **`lib/<area>/index.ts`** for a specific capability.

Package install and examples: [`../README.md`](../README.md).
