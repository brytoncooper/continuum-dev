{
  "name": "@continuum-dev/adapters",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/adapters/src",
  "projectType": "library",
  "tags": ["scope:adapters"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "packages/adapters",
        "commands": [
          "tsc --build --clean tsconfig.lib.json",
          "tsc --build tsconfig.lib.json"
        ]
      },
      "outputs": ["{workspaceRoot}/dist/packages/adapters"]
    },
    "nx-release-publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["^nx-release-publish"],
      "options": {
        "packageRoot": "dist/packages/adapters"
      }
    }
  }
}
